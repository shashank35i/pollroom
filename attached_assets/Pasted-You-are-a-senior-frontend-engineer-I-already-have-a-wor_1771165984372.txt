You are a senior frontend engineer. I already have a working React + Vite + TS frontend for a “Real-Time Poll Rooms” assignment, but it still looks like a template and contains misleading copy. 
Your task: MODIFY the EXISTING CODEBASE to become assessment-perfect: minimal, product-like, and aligned with requirements (poll creation, share link, join by link, real-time updates, fairness controls explained accurately, persistence-ready). Do NOT generate backend code.

Top priorities:
- Remove all template/boilerplate UI content (Profile/Settings/“Favorite Frameworks”/“Product Roadmap 2026”).
- Fix all misleading claims: NO “browser fingerprinting”, NO “all data stored in localStorage”, NO fake metrics like “0ms latency”, “∞ polls”.
- Keep the UI “big app” with clean SaaS dashboard styling, but every screen must relate to the poll product.
- Frontend must be backend-ready with clear REST + Socket.IO contracts and correct empty/loading/error states.

Keep stack:
React + Vite + TypeScript + Tailwind + react-router-dom + socket.io-client + zod + react-hook-form + @tanstack/react-query + lucide-react.

NAV / ROUTES (must match):
- Sidebar items ONLY:
  - Dashboard
  - Create Poll
  - Join Room
  - Recent Rooms (real: from localStorage of visited pollIds)
  - Notes (README/assignment notes page)
- Remove Profile and Settings routes entirely.
Routes:
- "/" Dashboard
- "/create" Create Poll
- "/join" Join Room
- "/p/:pollId" Poll Room
- "/notes" Notes (this is where fairness controls + edge cases + limitations are explained)
- "*" NotFound

DASHBOARD PAGE ("/"):
Make it action-first and credible:
- Top hero card: “Real-Time Poll Rooms” + 1 line subtitle.
- Replace the fake metric tiles with credible product indicators:
  - “Realtime: WebSocket” badge
  - “Persistence: Server-backed” badge
  - “Fairness: 2 controls” badge
(These are labels, not numeric claims.)
- Main grid:
  LEFT: Create Poll widget (same as Create page, but compact)
  RIGHT: Join Room card + Demo card + Recent Rooms card
- Demo card should say:
  “Demo uses the same Poll Room UI. Backend can provide a seeded pollId.”
Button navigates to /p/<demoId> using VITE_DEMO_POLL_ID fallback "demo".

CREATE POLL ("/create" and the widget):
- Strict Zod validation:
  question: trim, required, 10–140
  options: 2–6, each 1–60, no duplicates case-insensitive
- POST /api/polls -> { pollId, shareUrl }
- Success panel must include:
  - Copy Share Link
  - Copy Poll ID
  - Open Room
  - Copy Invite Message (client-only text)
- Handle error states + retry.

JOIN ROOM ("/join"):
- Input accepts full URL or pollId.
- Robust pollId extraction (supports your deployed URL format, /p/:pollId).
- Navigate to /p/:pollId.
- Inline validation + examples.

POLL ROOM ("/p/:pollId"):
Must clearly satisfy assessment:
- On mount: REST fetch GET /api/polls/:pollId (React Query)
- Then connect socket singleton and emit "poll:join" { pollId }
- Listen "poll:state" updates to update results in real-time.
- Connection status pill: LIVE / RECONNECTING / OFFLINE
- Manual Refresh button (REST refetch)
- Voting:
  - radio options
  - POST /api/polls/:pollId/vote { optionId }
  - handle 403 already voted (lock UI), 429 rate-limited, 404 not found
- Results:
  - counts + percentages + progress bars
  - “No votes yet” empty state
- Share actions:
  - Copy link (use current window origin + /p/:pollId if shareUrl absent)
  - Copy pollId
- Ensure there is NO copy claiming localStorage-only persistence.

NOTES PAGE ("/notes") — IMPORTANT:
This page must mirror submission README content in-app:
- Fairness controls implemented (frontend support + what backend should enforce):
  1) Stable anonymous clientId header (X-Client-Id) + backend vote lock per poll
  2) Rate limiting per IP/poll (backend) + UI cooldown message on 429
- Edge cases handled:
  - invalid pollId
  - 404 poll not found
  - already voted
  - reconnect behavior
  - double click protection
  - zero votes
- Known limitations:
  - clientId can be bypassed (incognito/new device)
  - IP rate limit affects shared networks
  - full bot protection needs captcha/auth (future)
Keep wording simple and honest.

CLIENT FAIRNESS SUPPORT (frontend):
- Create stable anonymous clientId in localStorage (uuid-like).
- Send X-Client-Id on all REST requests.
- Store voted marker localStorage["voted:<pollId>"]=optionId for instant UI lock.
- But ALWAYS treat server userStatus as source of truth when available.

API + SOCKET CONTRACTS (typed, centralized):
REST:
- POST /api/polls
- GET /api/polls/:pollId
- POST /api/polls/:pollId/vote
Socket:
- "poll:join" (client emit)
- "poll:state" (server emit)
- "poll:error" (server emit)
Make sure the code is ready for backend to implement these.

CLEANUP:
- Delete unused pages/components related to profile/settings/template recent activity.
- Remove any text mentioning “browser fingerprinting”.
- Remove “No data stored on server” from privacy. Either remove privacy page entirely or keep a short neutral note like:
  “We store polls/votes on the server for persistence. Client stores an anonymous id for vote fairness support.”
But prefer: remove privacy page and put relevant notes in /notes.
- Keep styling consistent and modern (sidebar + topbar, cards, gradients allowed).

Deliverables:
- Update the codebase accordingly.
- Ensure routes work and nav is correct.
- Ensure all copy aligns with the assignment requirements.
- Keep code clean, no dead components, no fake data.