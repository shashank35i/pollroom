Update ONLY navigation layout/behavior. Do NOT redesign pages. Implement:
1) Desktop: Gmail/ChatGPT-style collapsible left sidebar (expanded/collapsed) with a toggle.
2) Mobile: NO drawer. Instead show a fixed bottom navigation bar (like mobile apps).
CRITICAL: When the desktop sidebar is collapsed, clicking any nav item must navigate WITHOUT expanding the sidebar.

Assumptions: React + TypeScript + Tailwind + react-router-dom v6.

NAV CONFIG (single source of truth)
Create a `navItems` array used by both DesktopSidebar and MobileBottomNav:
- { key, label, to, icon }  (use lucide-react icons)

DESKTOP SIDEBAR (>= md)
- Two states: Expanded (~280px) and Collapsed (~64px).
- Persist `collapsed` in localStorage key: `ui.sidebarCollapsed`.
- Initialize from localStorage on first render to avoid flicker (lazy init function).
- Toggle button visible in sidebar header (and/or topbar) with aria-label.
- Use <NavLink> for nav items with active styles.
- Collapsed mode: icons only + `title` attribute tooltip.
- Expanded mode: icons + label.
- IMPORTANT: Clicking any nav item must NEVER modify `collapsed`. It only navigates.

MOBILE ( < md ) BOTTOM NAV
- Hide the left sidebar completely on mobile.
- Render a fixed bottom navigation bar:
  - height ~56-64px, full width, safe-area padding (pb-[env(safe-area-inset-bottom)] if desired).
  - 4–5 primary items max (e.g., Home/Landing, Rooms, Create, Join, Settings).
- Each tab navigates via <NavLink>.
- Active tab has clear highlight (icon + label).
- Bottom nav must not open any sidebar/drawer.
- Content area must have bottom padding so it isn’t hidden behind bottom nav.

LAYOUT
- AppShell uses responsive layout:
  - On desktop: grid with sidebar column + main column. Use CSS variable for sidebar width and transition width smoothly.
  - On mobile: single column main content, with bottom padding for the bottom nav.

FILES (only if needed)
- `src/components/layout/useNavUI.ts` (or context) to manage `collapsed` with localStorage.
- `src/components/layout/DesktopSidebar.tsx`
- `src/components/layout/MobileBottomNav.tsx`
- Update `AppShell.tsx` (or Layout) to conditionally render DesktopSidebar (md+) and MobileBottomNav (<md).

ACCEPTANCE TESTS
- Desktop collapsed -> click any nav item -> route changes, sidebar stays collapsed.
- Refresh page -> collapsed state preserved.
- Mobile -> bottom nav always visible, navigates correctly, no sidebar/drawer appears.
- Active route highlighting works on both.

Now implement the code changes with clean, production-ready TypeScript and Tailwind classes. Ensure `npm run dev` has zero TS errors.